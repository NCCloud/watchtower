---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: watchers.cloud.spaceship.com
spec:
  group: cloud.spaceship.com
  names:
    kind: Watcher
    listKind: WatcherList
    plural: watchers
    singular: watcher
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              destination:
                description: Destination sets where the rendered objects will be sent.
                properties:
                  bodyTemplate:
                    description: BodyTemplate is the template field to set what will
                      be sent the destination.
                    type: string
                  headerTemplate:
                    description: HeaderTemplate is the template field to set what
                      will be sent the destination.
                    type: string
                  method:
                    description: Method is the HTTP method will be used while calling
                      the destination endpoints.
                    type: string
                  urlTemplate:
                    description: URLTemplate is the template field to set where will
                      be the destination.
                    type: string
                type: object
              filter:
                description: Filter helps filter objects during the watching process.
                properties:
                  create:
                    description: Event allows you to set event based filters
                    type: string
                  update:
                    description: Object allows you to set object based filters
                    type: string
                type: object
              source:
                description: Source defines the source objects of the watching process.
                properties:
                  apiVersion:
                    description: APIVersion is api version of the object like apps/v1,
                      v1 etc.
                    type: string
                  concurrency:
                    description: Concurrency is how many concurrent workers will be
                      working on processing this source.
                    type: integer
                  hooks:
                    description: Options allows you to set source specific options
                    properties:
                      onSuccess:
                        description: OnSuccess options will be used when the source
                          is successfully processed.
                        properties:
                          delete:
                            description: Delete will delete the object after it successfully
                              processed.
                            type: boolean
                        type: object
                    type: object
                  kind:
                    description: Kind is the kind of the object like Deployment, Secret,
                      MyCustomResource etc.
                    type: string
                type: object
              valuesFrom:
                description: |-
                  ValuesFrom defines a list of sources (Secret/ConfigMap) to fetch values from.
                  They will be merged with the values provided in the Values field.
                items:
                  description: ValuesFrom defines a reference to a Secret or ConfigMap
                    to retrieve values.
                  properties:
                    key:
                      description: Key is the specific key within the Secret or ConfigMap
                        to retrieve the value from.
                      type: string
                    kind:
                      description: Kind specifies whether the source is a Secret or
                        ConfigMap.
                      type: string
                    name:
                      description: Name is the name of the Secret or ConfigMap.
                      type: string
                  required:
                  - key
                  - kind
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
